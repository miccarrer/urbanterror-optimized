# Copilot Instructions — Urban Terror Optimized

> **Source unique de vérité : [`CLAUDE.md`](CLAUDE.md).**
> Ce fichier ne duplique plus les règles pour éviter la dérive entre agents.

Lis `CLAUDE.md` à la racine du repo : il contient l'overview projet, le memory bank
à charger, les commandes de build, l'architecture, le style C, les conventions git
et la roadmap.

## Spécifique Copilot
- Au début de chaque tâche non triviale, lire `memory-bank/` (voir section *Memory Bank*
  de `CLAUDE.md`).
- Shell par défaut = **fish** (non POSIX) : préfixer les commandes complexes par `bash -c '...'`.
- Co-author trailer : `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>`.
